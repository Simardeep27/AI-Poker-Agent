# PPO-Trained Agent

Exported policy from the MaskablePPO training experiments. This version is kept for
comparison against the hand-designed EV agents and is benchmarked as `ppo` in the
results script.
